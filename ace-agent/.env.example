# ── OpenAI: Whisper transcription + TTS (onyx voice) ──
OPENAI_API_KEY=sk-proj-...

# ── Anthropic: Claude for non-command questions ──
ANTHROPIC_API_KEY=sk-ant-...

# ── Supabase: AceCRM (project kxtuegjptvzqycgyzehj) ──
SUPABASE_URL=https://kxtuegjptvzqycgyzehj.supabase.co
# Use the service_role key — server-side only, never ship to a browser.
# Find it in Supabase → Project Settings → API → service_role.
SUPABASE_KEY=eyJ...
