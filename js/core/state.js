// core/state.js — single source of truth for runtime state objects.
//
// Phase 2 placeholder. The legacy script still owns _store, _nav,
// _recentStore, _myRelationships, etc. Phase 3+ will lift them here
// and re-attach to window.* for the legacy script to read.
//
// Until then, importing from this module yields nothing useful —
// the file exists so the import path is reserved.
